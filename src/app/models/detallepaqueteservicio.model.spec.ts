import { Creatupropiopaquete } from './detallepaqueteservicio.model';

describe('Creatupropiopaquete', () => {
  it('should create an instance', () => {
    expect(new Creatupropiopaquete()).toBeTruthy();
  });
});
